# Requirements register

Superseded. The register is `docs/requirements.json`, rendered to
`docs/requirements.html` by `node tools/reqpage.mjs`, with a proof screenshot
per line captured by `node tools/proof.mjs`. See `HANDOFF.md` at the root.
